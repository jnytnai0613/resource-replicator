package constants

// CR metadata
const (
	// ClusterDetector
	Namespace    = "resource-replicator-system"
	FieldManager = "replicator-fieldmanager"
)

// Secret Info
const (
	IngressSecretName = "ca-secret"
	ClientSecretName  = "cli-secret"
)

// Ingress Info
const (
	IngressClassName = "nginx"
)
